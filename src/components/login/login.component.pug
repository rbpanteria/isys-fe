div(class='app-body')
  main(class='main d-flex align-items-center')
    div(class='container')
      div(class='row')
        div(class='col-md-6 mx-auto')
          div(class='card-group')
            div(class='card p-4')
              div(class='card-body')
                form('[formGroup]'='form' '(ngSubmit)'="authenticate()")
                  h1 Login
                  p(class='text-muted') Sign In to your account
                  div(class='input-group mb-3')
                    div(class='input-group-prepend')
                     span(class='input-group-text')
                        i(class='icon-user')
                    input(
                      type='text' 
                      class='form-control' 
                      placeholder='Username' 
                      autocomplete='username' 
                      'formControlName'='username'
                      required)
                    div('*ngIf'="controls.username.hasError('required')") Username is required
                  div(class='input-group mb-4')
                    div(class='input-group-prepend')
                      span(class='input-group-text')
                        i(class='icon-lock')
                    input(
                      type='password' 
                      class='form-control' 
                      placeholder='Password' 
                      autocomplete='current-password' 
                      id='password'
                      name='password'
                      'formControlName'='password'
                      required)
                    div('*ngIf'="controls.password.hasError('required')") Password is required
                  div(class='row')
                    div(class='col-6')
                      button(
                        '[disabled]'="!form.valid" 
                        class='btn btn-primary px-4') Login
                    div(class='col-6 text-right')
                      button(
                        type='button'
                        class='btn btn-link px-0') Forgot password?